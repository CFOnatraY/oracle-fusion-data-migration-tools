# Oracle Fusion API credentials and endpoints
# Copy this file as `.env` and fill in your own credentials.
# Never commit your `.env` file — keep it local and secure.

CLIENT_ID=your_client_id
CLIENT_SECRET=your_client_secret
TOKEN_URL=your_token_url
API_BASE_URL=your_api_base_url
BASE_URL=your_base_url
API_VERSION=your_api_version

FUSION_USERNAME=your_work_mail
FUSION_PASSWORD=your_work_pass